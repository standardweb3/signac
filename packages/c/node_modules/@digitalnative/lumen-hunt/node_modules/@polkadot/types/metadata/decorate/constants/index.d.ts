import type { MetadataLatest } from '../../../interfaces';
import type { Registry } from '../../../types';
import type { Constants } from '../types';
/** @internal */
export declare function decorateConstants(registry: Registry, { modules }: MetadataLatest, _metaVersion: number): Constants;
