import "source-map-support/register";
declare class LumenConfig {
    [key: string]: any;
    constructor({ dir }: {
        dir?: string;
    });
    eventManagerOptions(config: LumenConfig): any;
    static default(dir: any): LumenConfig;
}
export default LumenConfig;
