import type { MetadataLatest } from '../../../interfaces';
import type { Registry } from '../../../types';
import type { Errors } from '../types';
/** @internal */
export declare function decorateErrors(_: Registry, { modules }: MetadataLatest, metaVersion: number): Errors;
